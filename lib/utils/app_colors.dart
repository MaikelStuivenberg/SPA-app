import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // static const background = Color.fromRGBO(14, 106, 81, 1);
  static var mainColor = Colors.teal;
  static const secondaryColor = Colors.brown;
  // static const buttonColor = Color.fromRGBO(55, 180, 146, 1);
  // static const disabledButtonColor = Color.fromRGBO(157, 191, 182, 1);
}
